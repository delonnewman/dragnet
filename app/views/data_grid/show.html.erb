<%= render 'surveys/tabs', survey: grid.survey, selected: :table %>

<div class="container-fluid mt-3" hx-ext="morph">
  <div id="data-grid-table">
    <%= render 'data_grid/table', grid: grid %>
  </div>
</div>
