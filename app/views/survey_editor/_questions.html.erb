  <% questions = params[:removed] ? editor.survey.edited.questions : editor.survey.edited.questions.reject(&:removed?) %>
  <% questions.each do |question| %>
    <%= render 'survey_editor/question', question:, editor: %>
  <% end %>
